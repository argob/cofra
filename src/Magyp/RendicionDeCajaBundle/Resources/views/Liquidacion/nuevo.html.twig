{% extends 'MagypRendicionDeCajaBundle:Liquidacion:base.html.twig' %}

{% block cuerpo %}

    <h1>Liquidacion creation</h1>

    <form action="{{ path('sistema_liquidacion_create') }}" method="post" {{ form_enctype(form_liquidacion) }}>
        {{ form_widget(form_liquidacion) }}
        <br/>
        <hr/>
        
        Detalles
        <br/>
    {{ form_widget(form_detalle0) }}
        <br/>
        <hr/>
    {{ form_widget(form_detalle1) }}
        <p>
            <button type="submit">Create</button>
        </p>
    </form>

    <ul class="record_actions">
        <li>
            <a href="{{ path('sistema_liquidacion', { 'idrendicion': idrendicion }) }}">
                Volver
            </a>
        </li>
    </ul>
{% endblock %}