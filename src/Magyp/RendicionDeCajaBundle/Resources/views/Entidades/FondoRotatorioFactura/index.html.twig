<h1>FondoRotatorioFactura list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Numero</th>
            <th>Monto</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('entidades_fondorotatoriofactura_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.numero }}</td>
            <td>{{ entity.monto }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('entidades_fondorotatoriofactura_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('entidades_fondorotatoriofactura_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('entidades_fondorotatoriofactura_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
