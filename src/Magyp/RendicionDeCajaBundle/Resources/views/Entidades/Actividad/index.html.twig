<h1>Actividad list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Actividad_id</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('entidades_actividad_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.actividadid }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('entidades_actividad_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('entidades_actividad_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('entidades_actividad_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
