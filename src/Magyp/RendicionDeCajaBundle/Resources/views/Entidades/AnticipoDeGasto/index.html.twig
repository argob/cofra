<h1>AnticipoDeGasto list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Expediente</th>
            <th>Nota</th>
            <th>Fecha</th>
            <th>Monto</th>
            <th>Motivo</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('anticipodegasto_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.expediente }}</td>
            <td>{{ entity.nota }}</td>
            <td>{% if entity.fecha %}{{ entity.fecha|date('Y-m-d H:i:s') }}{% endif %}</td>
            <td>{{ entity.monto }}</td>
            <td>{{ entity.motivo }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('anticipodegasto_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('anticipodegasto_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('anticipodegasto_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
