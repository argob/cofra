<h1>Comprobante list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Numero</th>
            <th>Descripcion</th>
            <th>Importe</th>
            
        
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('comprobante_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.numero }}</td>
            <td>{{ entity.descripcion }}</td>
            <td>{{ entity.importe }}</td>
            
        
            <td>
                <ul>
                    <li>
                        <a href="{{ path('comprobante_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('comprobante_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('comprobante_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
