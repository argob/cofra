<h1>ReintegroDeGasto</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Expediente</th>
            <td>{{ entity.expediente }}</td>
        </tr>
        <tr>
            <th>Nota</th>
            <td>{{ entity.nota }}</td>
        </tr>
        <tr>
            <th>Periodoinicial</th>
            <td>{{ entity.periodoinicial|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Periodofinal</th>
            <td>{{ entity.periodofinal|date('Y-m-d H:i:s') }}</td>
        </tr>
        <tr>
            <th>Total</th>
            <td>{{ entity.total }}</td>
        </tr>
        <tr>
            <th>Fuentefinanciamiento</th>
            <td>{{ entity.fuentefinanciamiento }}</td>
        </tr>
        <tr>
            <th>Borrado</th>
            <td>{{ entity.borrado }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('sistema_entidades_reintegrodegasto') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('sistema_entidades_reintegrodegasto_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('sistema_entidades_reintegrodegasto_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
