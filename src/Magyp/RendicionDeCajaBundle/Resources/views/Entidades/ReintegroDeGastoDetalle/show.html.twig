<h1>ReintegroDeGastoDetalle</h1>

<table class="record_properties">
    <tbody>
        <tr>
            <th>Id</th>
            <td>{{ entity.id }}</td>
        </tr>
        <tr>
            <th>Importesubtotal</th>
            <td>{{ entity.importesubtotal }}</td>
        </tr>
    </tbody>
</table>

<ul class="record_actions">
    <li>
        <a href="{{ path('sistema_entidades_reintegrodegastodetalle') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('sistema_entidades_reintegrodegastodetalle_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>
        <form action="{{ path('sistema_entidades_reintegrodegastodetalle_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
