<h1>ReintegroDeGastoDetalle list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Importesubtotal</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('sistema_entidades_reintegrodegastodetalle_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.importesubtotal }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('sistema_entidades_reintegrodegastodetalle_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('sistema_entidades_reintegrodegastodetalle_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('sistema_entidades_reintegrodegastodetalle_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
