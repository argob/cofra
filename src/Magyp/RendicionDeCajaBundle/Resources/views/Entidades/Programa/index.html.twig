<h1>Programa list</h1>

<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Programa_id</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('programa_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.programaid }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('programa_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('programa_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('programa_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
