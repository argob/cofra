<h1>Rendicion edit</h1>

<form action="{{ path('sistema_rendicion_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <p>
        <button type="submit">Edit</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('rendicion') }}">
            Back to the list
        </a>
    </li>
    <li>
        <form action="{{ path('sistema_rendicion_delete', { 'id': entity.id }) }}" method="post">
            {{ form_widget(delete_form) }}
            <button type="submit">Delete</button>
        </form>
    </li>
</ul>
